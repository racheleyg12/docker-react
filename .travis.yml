sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t racheleyg12/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true racheleyg12/docker-react npm run test